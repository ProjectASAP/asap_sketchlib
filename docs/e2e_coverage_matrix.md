# E2E coverage matrix

This document maps the existing E2E suites. A family is an e2e_*.rs test file;
an algorithm, storage, path, or precision is a row.

The new Verified maximum error column is the upper bound established by a
successful test run. Most suites do not retain the exact observed maximum
(for example, 1.7%); that exact value is MISSING unless a row explicitly
reports it.

| Column | Meaning |
| --- | --- |
| Variant | Concrete implementation and configuration. |
| Input data | Distribution, element count, domain, seed, and ingestion form. |
| Family | Owning E2E suite. |
| Acceptance measurement | Exact comparison or numerical condition. 0 violations means every checked item met the condition; it does not mean every estimate had zero error. |
| Verified maximum error | The largest error the passing assertions prove is not exceeded, for the stated data/configuration. This is an upper bound, not the unrecorded exact maximum. |
| Bound or invariant | The error limit used by the test. |

In Count-Min rows, D is the number of distinct keys actually present in the
generated stream, and f_i is the exact frequency of the queried key.

## Frequency sketches

| Variant | Input data | Family | Acceptance measurement | Verified maximum error | Bound or invariant |
| --- | --- | --- | --- | --- | --- |
| Count-Min and Count Sketch, regular and fast paths, all matrix stores | Eight fixed keys: 1, 7, 4242, 90210, 1000003, 2147483647, 4294967311, 9007199254740993. Their counts are 10, 20, ..., 80: 360 updates total. | unit: src/sketches/countminsketch.rs, src/sketches/countsketch.rs | Both paths return each exact count: absolute error 0 for all checked answers. | 0 on all checked point estimates | Deterministic collision-free regression. Structural, so it is a unit test rather than an E2E row. |
| Count-Min Vector2D i64, fast path | Zipf(1.1), 100,000 updates, 8192-key domain, seed 1001; 4 x 4096; three shards. | e2e_frequency | Exact truth comparison per key; merged point estimates equal single-pass estimates. | <= stated Count-Min allowance for every checked key | Count-Min one-sided contract at 4 x 4096; matrix merge is exact addition. |
| Count-Min Vector2D i32, regular and fast paths | Three 120,000-update populations per path: Zipf integer keys over 8192 values, and uniform f64-encoded keys over 4096 values; 3 x 4096; seeds 1005+977t / 1006+977t, t=0,1,2. | e2e_frequency | Exact-key checks and 0 simultaneous violations. Marginal violations are evaluated as a binomial count over streams, not as independent keys from one stream. | <= stated Count-Min allowance for every checked key | est_i >= f_i; allowance e(N-f_i)/4096; marginal probability 4.98%. |
| CMSHeap, CSHeap, FoldCMS, FoldCS, portable CM/CS wrappers | Heap rows use Zipf(1.1), 40,000 updates, 4096 keys, seed 0x10BE_C700. Fold rows use a 16-way merge. Portable rows use deterministic string-key streams and wire round trips. | e2e_frequency, e2e_composition | Heap entry, point estimate, top-k recall, merged answer, and decoded answer are compared with exact truth appropriate to the row. | <= wrapped CMS/CS point-query bound | CMS inherits Count-Min; CS inherits Count Sketch; fold uses w shifted right by fold level; serialization is exact. |
| Count-Min Vector2D i64, regular and fast geometry/batch APIs | The 40,000-update Zipf(1.1), 4096-key, seed 0x10BE_C700 stream. Depths 1/2/3/9; widths 64/512/4096/8192; non-power-of-two widths 3/100/1000/4095. Pre-hashed, bulk, and weighted-batch entry points use a 4 x 2048 sketch over a 4,000-update Zipf(1.1) stream, 512-key domain, seed 0x10BE_C703. | e2e_frequency (geometry axis); unit: src/sketches/countminsketch.rs (entry points) | Every geometry satisfies one-sidedness and its simultaneous bound. Mean excess at widths 64, 512, 4096 falls by at least 2x per width step. Pre-hashed/batch answers equal a single-insert loop. | <= (D/.001)^(1/d)(N-f_i)/w simultaneously; 0 difference for equal-entry-point checks. |
| Count Sketch Vector2D i64, regular and fast geometry/batch APIs | The same 40,000-update Zipf stream. Depths 3/5/9; widths 64/512/4096/8192 and 3/100/1000/4095. Pre-hashed and weighted entry points use a 5 x 2048 sketch over a 4,000-update Zipf(1.1) stream, 512-key domain, seed 0x10BE_C704. | e2e_frequency (geometry axis); unit: src/sketches/countsketch.rs (entry points) | Every geometry satisfies its simultaneous L2 bound; each non-power-of-two width reports its requested width; pre-hashed/batch answers equal value entry points. | <= sqrt(kappa/w)*norm2(f_-i); 0 difference for equal-entry-point checks. |

Structural properties of the matrix families are unit tests, not E2E rows,
because they are exact equalities that hold for every stream and need no
ground truth or error bound:

| Property | Where |
| --- | --- |
| Collision-free exactness on both hashing paths, all stores | `src/sketches/countminsketch.rs`, `src/sketches/countsketch.rs` |
| Counter-width edges: i32/i64/i128 mass, signed cancellation, i128 merge | `src/sketches/countminsketch.rs`, `src/sketches/countsketch.rs` |
| Non-power-of-two column folding lands one counter per row | `src/sketches/countminsketch.rs` |
| `merge_max` dominance, idempotence, empty absorption, `max <= sum` | `src/sketches/countminsketch.rs` |
| Pre-hashed and bulk entry points equal the value entry points | `src/sketches/countminsketch.rs`, `src/sketches/countsketch.rs` |
| `CSHeap` i128 counters saturate into the heap at `i64::MAX` | `src/sketches/countsketch_topk.rs` |
| `CMSHeap` inert instances construct and report geometry (constructibility only) | `src/sketches/countminsketch_topk.rs` |

## Quantiles

| Variant | Input data | Family | Acceptance measurement | Verified maximum error | Bound or invariant |
| --- | --- | --- | --- | --- | --- |
| KLL f64 and KLLDynamic f64, k=64/200/800 | Four repeats of six 30,000-value streams: uniform integers [0,100,000,000); Normal(1000,250); Zipf(1.1), 8192 values mapped to [1,000,000,10,000,000]; 50-value duplicate-heavy; monotonic 0 through 29999; outside-in Normal(5000,900). Seed base 0xA5A50000+7919r. Fixed KLL: update, bulk, four-shard tree merge, eight-window tumbling merge. Dynamic KLL: first three feeds. | e2e_quantiles | At q={0,.01,.1,.5,.9,.99,1}, one sketch contributes its maximum normalized rank error. Success permits only the binomial number of independent sketches outside the limit. | <= 4.84% / 1.65% / 0.446% rank error | epsilon(k)=2.446/k^.9433: 4.84% (64), 1.65% (200), 0.446% (800). This is a DataSketches 99th-percentile characterization, not a theorem for this code. |
| KLL f64, k=64/256/1024 | 100,000 uniform values in [0,100,000,000), seed 0xC0FFEE01; four compaction seeds; q=.05,.10,...,.95. | e2e_quantiles | Largest rank error across all 76 queries must be inside epsilon(k); raising k from 64 to 1024 must reduce measured worst error by at least 4x. | <= 4.84% / 1.31% / 0.354% rank error | epsilon(64)=4.84%, epsilon(256)=1.31%, epsilon(1024)=0.354%; predicted 16x-capacity reduction is 13.4x. |
| KLL numeric-type variants | 14 numeric types; 20,000 uniform values per type within its representable span; seed 0x57EA0001; three compaction seeds; update, bulk, and four-shard feeds. Separate negative streams cover signed ordering. | e2e_numeric_types | Exact projected order statistics at seven q values; range and ordering checks. | <= 1.65% rank error | epsilon(200)=1.65% rank error after f64 projection; projection is exact below 2^53. |
| KLL f64 parameter axis | Uniform [0,1,000,000) streams. k=200 with m=2/4/8/16/64; k=8/16/32; 20,000 or 30,000 values and deterministic seeds. Exact regime: n=1/2/7/50/150 with k=200. | e2e_quantiles | m>=8 uses the normal k=200 rank test; m=2/4 uses a 3x wider band; small k uses its own epsilon; n<k returns exact order statistics. Invalid m/k geometry is normalized and reported. | <=1.65% for k=200,m>=8; <=4.96% for m=2/4; <=34.4%/17.9%/9.28% for k=8/16/32; 0 for n<k. |
| Core DDSketch and portable DdSketch, alpha=.001/.01/.05/.1 | For each alpha and n=1,000/20,000/100,000: bucket-edge log-uniform values (exponents 5..39); positive Normal(1000,250); Exponential(rate .001); uniform [1,000,000,10,000,000); Zipf(1.1) over 8192 values in that range; nine-decade values. Seed 3005000+101*size_index+1000000*alpha. | e2e_quantiles | Seven q values are compared with the exact order statistic under each implementation's rank convention. Successful run has 0 relative-value-bound violations. | <= alpha + ULP: 0.1%, 1%, 5%, or 10%, as configured | Relative value error <= alpha plus logarithmic-mapping ULP slack: 0.1%, 1%, 5%, or 10%. |
| Core DDSketch and portable DdSketch, extreme alpha | 20,000-value deterministic streams at alpha=.00001/.0001/.3/.5/.9; values outside the implementation's indexable range are excluded before truth is built. | e2e_quantiles | Each implementation retains every indexable value and meets its own order-statistic relative-error test. Bucket count must not increase as alpha becomes looser. | <= alpha + ULP: 0.001%, 0.01%, 30%, 50%, or 90%. |
| DDS numeric types and windowed KLL/DDS | DDS: 14 numeric types, alpha=.001/.01/.05. Windows: 10,000 uniform values, seed 0x0E110001; KLL domain [0,1,000,000), DDS domain [0,9,000,000), truth is the retained bucket span. | e2e_numeric_types, e2e_windows | Every query compares with the exact projected/retained order statistic; 0 DDS violations. | <= alpha + ULP: 0.1%, 1%, 5%, or 10%, as configured | DDS <=alpha+ULP; windowed KLL epsilon(200)=1.65%. |
| UnivMonQ | Three 200,000-value streams: uniform [0,10,000,000), seed 0x0DDE0001; Zipf(1.4), 4096 values in [1,1,000,000], seed 0x0DDE0002; 60,000 Zipf(1.6) values over 64 plus 140,000 uniform values, seeds 0x0DDE0003/4. Twelve hash/source trials per regime. | e2e_quantiles | Exact CDF and rank checks over truth support and estimated breakpoints; violations are tallied per independent sketch. | <= configured CDF/rank bound | sup_x abs(Fhat-F) <= 2E_H + P_R*sqrt(log(2/delta)/(2m_R)). |

## Cardinality, membership, and sampling

| Variant | Input data | Family | Acceptance measurement | Verified maximum error | Bound or invariant |
| --- | --- | --- | --- | --- | --- |
| HLL Classic, p12/p14/p16 | Seven separate unique-ID streams: n=10,100,1,000,10,000,100,000,1,000,000,10,000,000. Stream i contains IDs from (i+1)*2^40 through (i+1)*2^40+n-1, so streams have no shared IDs. The largest stream replays its first 200,000 IDs. A separate 200,000-ID stream is split into even/odd shards and merged. | e2e_cardinality | Maximum observed error: MISSING. Seven exact-cardinality comparisons with 0 accepted band violations. Duplicate replay leaves estimate bit-identical. Merging the two 100,000-ID shards must reproduce every register and the estimate of a one-pass 200,000-ID sketch exactly. | <= +/-6.50% / +/-3.25% / +/-1.625% relative error | Four standard deviations of 1.04/sqrt(2^p): +/-6.50% (p12), +/-3.25% (p14), +/-1.625% (p16). |
| HLL ErtlMLE, p12 | Seven separate unique-ID streams at n=10,100,1,000,10,000,100,000,1,000,000,10,000,000. Stream i contains IDs from (i+1)*2^40 through (i+1)*2^40+n-1. The n=10,000,000 stream replays 200,000 IDs; an additional 200,000-ID stream is split into even/odd shards. | e2e_cardinality | Seven exact-cardinality comparisons with 0 accepted band violations; duplicate replay leaves the estimate unchanged; merging the shards reproduces one-pass registers and estimate exactly. | <= +/-6.50% relative error | Four standard deviations of 1.04/sqrt(2^12). |
| HLL ErtlMLE, p14 | Seven separate unique-ID streams at n=10,100,1,000,10,000,100,000,1,000,000,10,000,000. Stream i contains IDs from (i+1)*2^40 through (i+1)*2^40+n-1. The n=10,000,000 stream replays 200,000 IDs; an additional 200,000-ID stream is split into even/odd shards. | e2e_cardinality | Seven exact-cardinality comparisons with 0 accepted band violations; duplicate replay leaves the estimate unchanged; merging the shards reproduces one-pass registers and estimate exactly. | <= +/-3.25% relative error | Four standard deviations of 1.04/sqrt(2^14). |
| HLL ErtlMLE, p16 | Seven separate unique-ID streams at n=10,100,1,000,10,000,100,000,1,000,000,10,000,000. Stream i contains IDs from (i+1)*2^40 through (i+1)*2^40+n-1. The n=10,000,000 stream replays 200,000 IDs; an additional 200,000-ID stream is split into even/odd shards. | e2e_cardinality | Seven exact-cardinality comparisons with 0 accepted band violations; duplicate replay leaves the estimate unchanged; merging the shards reproduces one-pass registers and estimate exactly. | <= +/-1.625% relative error | Four standard deviations of 1.04/sqrt(2^16). |
| HLL HIP, p12/p14/p16 | Seven disjoint streams at n=10 through 10,000,000; largest stream replays 200,000 existing IDs. HIP has no merge API. | e2e_cardinality | Seven exact-cardinality comparisons with 0 accepted band violations; duplicate replay leaves estimate unchanged. | <= +/-5.20% / +/-2.60% / +/-1.30% relative error | Four standard deviations of sqrt(ln(2)/2^p): +/-5.20% (p12), +/-2.60% (p14), +/-1.30% (p16). |
| Portable HllSketch Regular/Datafusion/Hip tags, p12/p14/p16 | Nine independent 200,000-value uniform u64 streams in [0,2^62), seeds 2001 through 2009; each is shard-merged. | e2e_cardinality | Exact HashSet cardinality truth; no accepted band violations; shard merge equality. | <= +/-6.50% / +/-3.25% / +/-1.625% relative error | All portable tags use the register estimator: +/-6.50%, +/-3.25%, +/-1.625%. |
| HLL RSE and switchover | Six blocks per precision; switchover n is 10,240 (p12), 40,960 (p14), 163,840 (p16). | e2e_cardinality | RSE <=2x asymptotic prediction and p12 RSE >=2x p16. Recorded switchover RSE/prediction ratios: 2.1x/3.5x/6.3x, about 3.41%/2.84%/2.56% RSE. | RSE <= 2x prediction; measured switchover RSE: 3.41% / 2.84% / 2.56% | Asymptotic RSE 1.04/sqrt(m); empirical switchover envelope 1.5x to 10x prediction. |
| HLL Classic, ErtlMLE, and HIP custom precisions p10/p13/p18 | Separate unique-ID checkpoint streams: p10 n=100/1000/20000/200000; p13 n=1000/10000/100000/500000; p18 n=10000/100000/1500000. Custom-register merge is checked against a single pass. | e2e_cardinality | Each estimator/precision has 0 accepted confidence-band violations; merge reproduces one-pass registers and estimate. Custom p10/p13/p18 RSE decreases in the predicted order. | Classic/Ertl: +/-13.0% / +/-4.60% / +/-0.813%; HIP: +/-10.4% / +/-3.68% / +/-0.650%. |
| KMV k=64/1024/4096 | n<k, n=k, n>k; eight hash seeds 0,1,2,3,7,11,13,17; identity ranges separated by 2^40. | e2e_experimental | Exact below k; active estimator at/above k; duplicate and merge equality. | 0 below k; <= 4sigma relative band at/above k | Exact n<k; 4sigma relative band 4sqrt((n-k+1)/(n(k-2))) for n>=k. |
| Bloom regular/fast; Nitro; UniformSampling | Bloom uses inserted sets and disjoint probes across geometries. Nitro: rates 1/.5/.3/.1/.07/.01, 12 seeds, true count round(1000/rate); Zipf(1.1), n=200,000, domain=512, seed 0x01172199; and CM/CS integration at rate 1 or seeded sampling, n=40,000, domain=2048, 4 x 2048. UniformSampling: rates 1/.5/.25/.1/.01; n=0/1/7/1000/10000/50000; mean test has 20,000 values i^1.7 and 24 seeds. | e2e_membership, e2e_nitro, e2e_experimental | Bloom: 0 false negatives and geometry-derived FPP comparison. Nitro: every row/mean inside sampling band, exact at rate 1; disable/replay/serde preserve the seeded sequence. UniformSampling: retained count=ceil(n*rate), multiplicity preserved, mean in 4sigma interval. | 0 false negatives; false positives <= geometry-derived FPP acceptance | Bloom FPP from rounded geometry. Nitro 4sigma sampling band plus sketch slack. SRSWOR variance (sigma_N^2/m)(N-m)/(N-1). |

## Heavy hitters, frameworks, windows, composition, and Octo

| Variant | Input data | Family | Acceptance measurement | Verified maximum error | Bound or invariant |
| --- | --- | --- | --- | --- | --- |
| SpaceSaving | Zipf(1.1), 60,000 updates, 2048 keys, seed 9001; capacities including 256 and >2048; weighted, merge, malformed-state, and serde cases. | e2e_heavy_hitters | Monitored keys are checked against exact count/error; unmonitored exact count is bounded by current minimum; oversized capacity is exact. | 0 outside its exact count/error sandwich | count <= estimate <= count+error; dropped count <= minimum tracked count. |
| Coco and Elastic | Coco: Zipf(1.2), 40,000 updates/2000 keys, seed 4242; Zipf(1.1), 60,000/4000, seed 909; 800-run unbiasedness and 200-run recall, including partial-key queries. Elastic uses deterministic elephant/light eviction and heavy-part maintenance streams. | e2e_heavy_hitters | Coco mass partition/heavy ceiling and Elastic no-underestimate/ceiling use exact truth; partial-key and maintenance operations preserve their mass/accounting invariants. | <= stated one-sided ceiling | Coco estimate <= f+e(N-f)/w; Elastic f <= estimate <= f+e(N-f)/light_cols. |
| Hydra cells and EHSketchList | Hydra uses labelled, seeded Zipf records over 4 regions, 4 endpoints and status/device dimensions. EH uses 10,000 updates: Zipf(1.1), 2048 keys, seed 0x0E110001; numeric streams are uniform. | e2e_frameworks, e2e_windows | Each lattice cell/window is scored against its own exact retained truth; query facades, routing, merge, serialization, expiry, merge norm, and sketch-pool reuse are checked. | <= stated bound | Contained sketch contract; retention adds no tolerance. |
| TumblingWindow and composition wrappers | Rotating windows use Zipf(1.1), 512 keys, seed 0x0E110001, and uniform numeric [0,100,000). Composition frequency stream: Zipf(1.1), 40,000 updates, 2048 keys, seed 0xC0905101. | e2e_windows, e2e_composition | Current-window truth; standalone/member equality; wire round trips; incompatible layout rejection. | 0 for the stated exact-equality checks | Wrapped sketch contract; routing and serialization invariant is exact. |
| Count-Min Octo plans | Zipf(1.1), 400,000 updates, 8192-key domain, seed 11001; both HashByKey and round-robin routes; compared with a single-thread Count-Min reference. | e2e_octo | Point estimates are compared with exact truth and reference after delivery. | <= stated Count-Min allowance for every checked key | reference-k*tau <= octo <= reference, plus the Count-Min bound. |
| Count Sketch Octo plans | Zipf(1.1), 400,000 updates, 8192-key domain, seed 11003; both HashByKey and round-robin routes; compared with a single-thread Count Sketch reference. | e2e_octo | Point estimates are compared with exact truth and reference after delivery. | <= sqrt(kappa/w)*norm2(f_-i) for every checked key | abs(octo-reference) <= k*tau, plus the Count Sketch L2 bound. |
| HLL Octo plan | Zipf(1.1), 40,000 updates, 2048-key domain, seed 11005; both routes; compared with a single-thread HLL reference. | e2e_octo | Registers after flush must equal the single-thread registers; estimate is compared with exact distinct count. | <= stated bound | Exact register transport plus the HLL cardinality band. |
| DDSketch Octo plan | Zipf(1.1), 60,000 updates, 4096-key domain, seed 11007; both routes; compared with a single-thread DDSketch reference. | e2e_octo | Buckets after flush must equal single-thread buckets; quantiles use exact order statistics. | <= alpha + ULP: 0.1%, 1%, 5%, or 10%, as configured | Exact bucket transport plus alpha-relative value error. |
| UnivMon Octo plan | Zipf(1.1), 40,000 updates, 2048-key domain, seed 11009; both routes; compared with exact frequency truth. | e2e_octo | Delivered L1 accounting is exact; F2 is compared with exact F2. | <= stated bound | Exact delivered accounting plus the AMS F2 bound. |
| Coco/Elastic Octo and runtime | Deterministic Zipf(1.1) worker streams under both routes; exact top-32 truth; replay uses identical schedule/input. | e2e_octo | Coco estimates are one-sided and <=N; Elastic is in [0,N]; runtime replay is cell-for-cell equal. | <= stated one-sided ceiling | Single-thread Coco/Elastic contracts; deterministic transport equality. |
| DataInput hashing across Count-Min, Count Sketch, Bloom, HLL, SpaceSaving, KLL, and DDSketch | Every public DataInput variant; hash seeds 0/1/3/7. Signed/unsigned aliases, 128-bit values, floats, strings, bytes, and numeric/non-numeric quantile inputs. | e2e_data_input | Canonical aliases have identical digests; distinct encodings do not alias; sketch answers reflect those identities; quantile sketches reject non-numeric inputs. | 0 digest/answer difference where equality is required; 0 Bloom false negatives. |
| ASAPv1 wire envelopes | Deterministic frequency and numeric streams exercise DDSketch, Bloom, Coco, Elastic, SpaceSaving, KLLDynamic, matrix heaps, EH/EHSketchList, Hydra, UnivMon, experimental sketches, and all matrix/quantile envelopes. | e2e_wire | Envelope family/variant tag is exact; only the owning decoder accepts it; decoded answers, counters, registers, buckets, and serialized deltas match the pre-wire object. | 0 wire round-trip difference for every checked state and answer. |
